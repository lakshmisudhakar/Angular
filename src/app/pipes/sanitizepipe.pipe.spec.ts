import { SanitizepipePipe } from './sanitizepipe.pipe';

describe('SanitizepipePipe', () => {
  it('create an instance', () => {
    const pipe = new SanitizepipePipe();
    expect(pipe).toBeTruthy();
  });
});
